import Ember from "ember";
import Adapter from "ember-data/adapters/json-api";

const { Inflector } = Ember;

export function initialize(application) {
}

export default {
	name: 'inflector-rules-initializer',
	initialize
};
