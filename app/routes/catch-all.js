import Ember from 'ember';

export default Ember.Route.extend({
	
	/**
	 * This is a catch all page if a user visits a page that's not a route
	 * Redirect them back to the index page 
	 */
	//redirect: function() {
    //    this.transitionTo('index'); 
    //}
	
});
